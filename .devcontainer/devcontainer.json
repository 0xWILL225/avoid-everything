{
  "name": "Avoid Everything Dev Container",
  "dockerComposeFile": ["docker-compose.yaml"],
  "service": "avoid-everything-dev",
  "workspaceFolder": "/workspace",
  
  // Configure tool-specific properties
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.pylint",
        "ms-python.black-formatter",
        "ms-toolsai.jupyter",
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/bin/python3",
        "python.linting.enabled": true,
        "python.linting.pylintEnabled": true,
        "python.formatting.provider": "black"
      }
    }
  },
  
  // Add features to the dev container
  "features": {
    "ghcr.io/devcontainers/features/git:1": {},
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": false,
      "installOhMyZsh": false,
      "upgradePackages": false
    }
  },
  
  // Set environment variables
  "remoteEnv": {
    "PYTHONPATH": "/workspace:${PYTHONPATH}",
    "DISPLAY": "${localEnv:DISPLAY}"
  },
  
  // Set git safe directory and install robofin
  "postCreateCommand": "git config --global --add safe.directory /workspace && pip install -e /workspace/robofin",
  
  // Keep container running
  "shutdownAction": "stopCompose"
} 